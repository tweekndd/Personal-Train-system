## MiniQbot-Lite V0.1 依赖
akshare>=1.14.0
efinance>=0.4.9        # 备选数据源，akshare 失效时自动回退
pandas>=2.0.0
numpy>=1.24.0
sqlalchemy>=2.0.0
loguru>=0.7.0

# 测试
pytest>=8.0.0

# 可视化
# mplfinance 目前仅 alpha/beta 版本，装不上就用纯文本
# pip install mplfinance 如果失败不影响核心功能
